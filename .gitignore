# Logs y SO
*.log
.DS_Store
Thumbs.db

# Entornos / secretos
.env
.env.backup
.env.production

# Herramientas/IDE
/.idea
/.vscode
/.fleet
/.nova
/.zed
/.phpunit.cache
/.phpunit.result.cache
/phpactor.json
/auth.json

# Dependencias locales
/node_modules
/vendor

# Laravel (archivos generados en runtime)
#   - Mantenemos public/build (ver excepciones al final)
/public/hot
/public/storage
/storage/*.key
/storage/pail

# Homestead / Vagrant
Homestead.json
Homestead.yaml

# ====== EXCEPCIONES (sí subir a Git) ======
# Subir el build de Vite (manifest + assets)
!public/build/
!public/build/**

# Si en algún momento decides versionar el storage público enlazado (no usual):
# !public/storage/
